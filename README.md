<div align="center">

  <h1>🚀 Проект c2desf: Нова Ера для C-коду 🚀</h1>
  <p>
    <strong>Революційний підхід до компіляції, оптимізації та виконання C-коду. Платформонезалежний, безпечний та надзвичайно ефективний формат <code>.desf</code> та утиліта для його генерації <code>c2desf</code>.</strong>
  </p>

  <p>
    <a href="#-вступ-нова-парадигма-для-c-коду--формат-desf">Вступ</a> •
    <a href="#-ключові-особливості-та-переваги">Особливості</a> •
    <a href="#-концепція-desf-виконання-через-інтерпретацію-команд">Концепція</a> •
    <a href="#-формат-desf-детально">Формат .desf</a> •
    <a href="#-утиліта-c2desf-автоматизація-перетворення">Утиліта c2desf</a> •
    <a href="#-віртуальна-машина-vm-для-desf">VM</a> •
    <a href="#-стратегії-оптимізації-та-захисту">Оптимізація та Захист</a> •
    <a href="#-універсальність-та-портативність">Портативність</a> •
    <a href="#-встановлення-та-використання">Встановлення</a> •
    <a href="#-ліцензія">Ліцензія</a>
  </p>
</div>

## 🌟 Вступ: Нова парадигма для C-коду – формат `.desf`

У світі розробки програмного забезпечення постійно виникає потреба в оптимізації та універсальності. Традиційні підходи до компіляції C-коду у вигляді динамічних бібліотек (`.so` для Linux/Unix, `.dll` для Windows) мають суттєві недоліки: вони залежать від конкретної операційної системи та архітектури процесора, що значно ускладнює переносимість та розгортання.

**Саме для вирішення цих проблем виникла ідея створення `.desf` (Description File) – нового, платформонезалежного проміжного формату.**

> Основна місія `.desf` – забезпечити **оптимізацію виконання** та **повну незалежність від платформи**, відмовляючись від стандартних скомпільованих бібліотек. Цей підхід особливо актуальний для покращення роботи з великими скриптами, наприклад, у середовищі BYM, та для запуску коду на найрізноманітніших пристроях, від потужних серверів до вбудованих систем і навіть IoT-пристроїв (так, ми говоримо про той самий "холодильник"!).

Проект `c2desf` включає в себе:
1.  **Специфікацію формату `.desf`**: Детальний опис інноваційного представлення C-коду.
2.  **Утиліту `c2desf`**: Інструмент для автоматичного перетворення вихідного C-коду у формат `.desf`.
3.  **Віртуальну машину (VM) `.desf`**: Високопродуктивний інтерпретатор для виконання файлів `.desf` на будь-якій підтримуваній платформі.

## ✨ Ключові особливості та переваги

* 🌐 **Платформонезалежність**: Запускайте ваш C-код будь-де! Від Windows, Linux, macOS до Android, iOS та мікроконтролерів. `.desf` усуває необхідність компіляції під кожну окрему архітектуру.
* 🚀 **Оптимізація виконання**:
    * **Розмір**: Агресивна мініфікація, інтелектуальне видалення невикористаного коду та компактне представлення команд значно зменшують розмір кінцевого файлу (потенційно до 30-50% економії порівняно з оригінальним C).
    * **Швидкість**: Використання SIMD-інструкцій (NEON, AVX2), кешування та потенційна JIT-компіляція "гарячих" ділянок коду забезпечують високу продуктивність.
* 🛡️ **Посилений захист**:
    * **Багаторівнева обфускація**: Перейменування ідентифікаторів, шифрування рядків, впровадження "мутного коду", Control Flow Flattening.
    * **Анти-налагоджувальні прийоми**: Ускладнення аналізу та реверс-інжинірингу.
    * **Нестандартний формат**: Сам формат `.desf` є бар'єром для стандартних інструментів декомпіляції.
* 💡 **Інноваційна концепція**: Відмова від традиційних бінарних бібліотек на користь інтерпретованого, але оптимізованого представлення.
* 🔧 **Автоматизація**: Утиліта `c2desf` автоматизує весь процес перетворення.
* 🧩 **Модульність та розширюваність**: Чітка структура проекту та потенціал для плагінів.
* 🔒 **Безпека для IoT**: Комплексні заходи для безпечного виконання коду на підключених пристроях, включаючи криптографію, автентифікацію та ізоляцію.

### Чому `.desf` краще за традиційні `.so`/`.dll`?

| Характеристика        | Традиційні `.so`/`.dll`                 | Формат `.desf`                                                                 |
| :-------------------- | :-------------------------------------- | :----------------------------------------------------------------------------- |
| **Розмір** | Часто великий, залежить від компілятора | **Значно менший** завдяки оптимізаціям та компактному представленню команд. |
| **Захист від аналізу** | Відносно легко декомпілюються (IDA Pro, Ghidra) | **Суттєво вищий рівень захисту** через обфускацію та невідомий байт-код.   |
| **Платформонезалежність** | Відсутня, потрібна окрема компіляція   | **Повна**, завдяки інтерпретатору/VM.                                         |
| **Переносимість** | Обмежена архітектурою та ОС             | **Максимальна**, від серверів до мікроконтролерів.                             |

## 🧠 Концепція `.desf`: Виконання через інтерпретацію команд

На відміну від прямої компіляції в машинний код для конкретної архітектури, `.desf` використовує інший підхід.

> Файл `.desf` – це не бінарний файл незрозумілого формату, а **спеціальним чином структурований C-код** (або текстово-бінарний/бінарний файл у фінальній версії). Його ключова особливість полягає в тому, що вся логіка вихідної C-програми перетворюється на **послідовність простих команд**.

Ці команди зберігаються у масиві всередині згенерованого файлу (якщо це C-код) або у спеціалізованій структурі (якщо це бінарний формат). Виконання програми відбувається шляхом ітерації по цьому масиву команд у циклі `for` (у концептуальному C-представленні) та їх інтерпретації за допомогою великої конструкції `switch-case` у віртуальній машині `.desf`. Кожен `case` відповідає за виконання певної команди (наприклад, присвоїти значення змінній, викликати функцію, перейти до іншої частини коду).

### Оптимізація та обфускація на рівні концепції

Для досягнення високої продуктивності та зменшення розміру коду, а також для ускладнення аналізу, застосовується обфускація та оптимізація ще на етапі генерації `.desf`:

* **Скорочення імен**: Усі імена змінних та функцій замінюються на односимвольні або короткі ідентифікатори (наприклад, `myCounter` → `@a`, `calculateSum` → `F1`). Це реалізується за допомогою `#define` макросів у проміжному C-коді або зберігається у таблиці символів `.desf` файлу.
* **Командна структура**: Логіка програми (оператори, виклики функцій) перетворюється на числові коди команд, які зберігаються в `enum` (наприклад, `enum CMD { SET_A=0, PRINT=1, ADD=2, JUMP_IF_ZERO=3, EXIT=4 };`).

Такий підхід дозволяє стандартному C-компілятору (наприклад, `gcc`), який використовується для компіляції самої VM, ефективно оптимізувати згенерований `switch-case` код, використовуючи всі можливості цільової архітектури (кешування, передбачення переходів тощо), при цьому сам `.desf` файл залишається компільованим стандартними засобами без зовнішніх залежностей (або виконується VM без попередньої компіляції на цільовій платформі).

## 📄 Формат `.desf` детально

Формат `.desf` є основою всього проекту. Його специфікація визначає, як C-код трансформується у набір команд, зрозумілих для віртуальної машини.

### Приклад структури `.desf` файлу (концептуальне C-представлення)

Це спрощений приклад, що ілюструє ідею. Реальний `.desf` може бути бінарним.

```c
// Файл: program.desf (компілюється як звичайний C-файл: gcc program.desf -o program)
// АБО: цей файл є вхідними даними для VM .desf

#include <stdio.h> // Включаємо стандартні бібліотеки за потреби (у VM це буде абстраговано)

// --- Обфускація через макроси (або таблиця символів у .desf) ---
#define var_a vm_registers[0] // Змінна 'counter' тепер 'var_a' (елемент масиву регістрів VM)
#define var_b vm_registers[1] // Інша змінна
#define F_print printf      // Функція 'printf' тепер 'F_print' (виклик зовнішньої функції через VM)

// --- Коди команд (визначені у специфікації .desf) ---
enum DESF_CMD {
    CMD_SET_VAR = 0,   // Команда: встановити значення змінної (регістра)
    CMD_ADD_VARS = 1,  // Команда: додати дві змінні, результат у першу
    CMD_PRINT_VAR = 2, // Команда: надрукувати змінну
    CMD_EXIT_VM = 3    // Команда: завершити виконання VM
    // ... та багато інших команд для циклів, умов, викликів функцій тощо
};

// --- Масив для зберігання значень обфускованих змінних (регістри VM) ---
// int vm_registers[MAX_REGISTERS] = {0}; // Управляється VM

// --- Послідовність команд та їх аргументів (серце .desf файлу) ---
// Кожна команда може мати різну кількість аргументів.
// Наприклад: { CMD_ID, ARG1_TYPE, ARG1_VAL, ARG2_TYPE, ARG2_VAL, ... }
// ARG_TYPE може бути: CONSTANT, REGISTER_IDX, LABEL_IDX 등.
int desf_commands[] = {
    CMD_SET_VAR, /*reg_idx*/ 0, /*value*/ 5,     // var_a = 5
    CMD_SET_VAR, /*reg_idx*/ 1, /*value*/ 10,    // var_b = 10
    CMD_ADD_VARS, /*dest_reg*/ 0, /*src_reg1*/ 0, /*src_reg2*/ 1, // var_a = var_a + var_b
    CMD_PRINT_VAR, /*reg_idx*/ 0,                 // F_print("...", var_a)
    CMD_EXIT_VM
};

int desf_command_count = sizeof(desf_commands) / sizeof(int); // Не зовсім так, залежить від структури

// --- Основна логіка виконання (реалізована у VM .desf) ---
/*
int main_vm_loop() {
    int pc = 0; // Лічильник команд (Program Counter) у VM

    // Цикл виконання команд VM
    while (desf_commands[pc] != CMD_EXIT_VM && pc < desf_command_count) { // Умова виходу може бути складнішою
        int current_command_code = desf_commands[pc];
        switch (current_command_code) {
            case CMD_SET_VAR:
                // vm_registers[desf_commands[pc+1]] = desf_commands[pc+2];
                // pc += 3; // Перейти до наступної команди + аргументи
                break;
            case CMD_ADD_VARS:
                // vm_registers[desf_commands[pc+1]] = vm_registers[desf_commands[pc+2]] + vm_registers[desf_commands[pc+3]];
                // pc += 4;
                break;
            case CMD_PRINT_VAR:
                // F_print("Result: %d\n", vm_registers[desf_commands[pc+1]]);
                // pc += 2;
                break;
            // ... інші case для інших команд ...
            default:
                // Обробка невідомої команди (опціонально)
                // F_print("Unknown command: %d\n", desf_commands[pc]);
                return 1; // Помилка
        }
    }
    return 0; // Успішне завершення
}
*/
```

Специфікація команд (commands.h)
Ключовим для узгодженої роботи генератора (c2desf) та інтерпретатора (VM) є файл include/desf/commands.h (або аналогічний файл специфікації), який визначає "контракт" формату .desf.
Основні елементи специфікації:
 * Версія формату та "магічне число": Для ідентифікації та сумісності.
   #define DESF_VERSION 0x00010000 // Версія 1.0.0
#define DESF_MAGIC   0xDEADBEEF // "Магічне" число для ідентифікації файлу

 * Обмеження: Наприклад, максимальна кількість команд, довжина операндів/міток.
 * Типи команд (DesfCommandType): Розширений перелік команд, що охоплює:
   * Арифметичні операції (цілочисельні, з плаваючою комою, векторні).
   * Логічні операції.
   * Операції порівняння.
   * Операції керування потоком (безумовні та умовні переходи, виклики підпрограм, повернення).
   * Операції для циклів (початок, кінець, ітерація).
   * Операції роботи з пам'яттю (завантаження, збереження, виділення).
   * Операції вводу/виводу (абстраговані від конкретної реалізації stdio).
   * Команди налагодження (напр., DESF_BREAKPOINT).
 * Структура команди (DesfCommand): Визначає, як кожна команда зберігається у файлі.
```c
   // Приклад можливої структури команди
typedef struct {
    uint8_t type;     // Тип команди (з DesfCommandType)
    uint8_t flags;    // Додаткові прапорці для команди
    union {
        struct { // Для команд з операндами
            DesfOperand op1;
            DesfOperand op2;
            DesfOperand op3; // Деякі команди потребують 3 операнди
        } operands;
        char* label; // Для команд переходу (мітка)
        // ... інші варіанти даних
    } data;
} DesfCommand;

// Приклад структури операнда
typedef struct {
    DesfOperandType type; // Тип операнда: константа, регістр, адреса пам'яті, мітка
    union {
        int32_t constant_val_i32;
        float   constant_val_f32;
        uint16_t register_idx;
        uint32_t memory_addr;
        char* label_name;
        // ...
    } value;
} DesfOperand;
```

 * Структура заголовка файлу (DesfHeader): Містить метадані (версія, магічне число, розміри секцій, таблиця символів, таблиця рядків тощо).
 * Макроси для генерації: Зручні C-макроси (DESF_SET, DESF_ADD тощо) для спрощення генерації коду (якщо .desf генерується як C-файл).
Еволюція commands.h та "проактивні" покращення:
У процесі розробки специфікація формату зазнала змін для покращення:
 * Вирішення проблеми stdio: Реалізація функцій вводу/виводу винесена в окремий модуль desf_stdio.c з умовною компіляцією (#ifdef DESF_STANDALONE) для повної незалежності від стандартної бібліотеки C на цільовій платформі.
 * Коректні операнди: Структура DesfCommand була адаптована для підтримки до трьох операндів, необхідних для деяких арифметичних команд.
 * Оптимізація структури: Використання union для полів операндів та мітки для ефективнішого використання пам'яті.
 * Документація: Детальні коментарі до кожного DesfCommandType з поясненням призначення та очікуваних операндів.
 * Підтримка плагінів: Механізм реєстрації зовнішніх функцій (DesfPluginFunc), що дозволяє розширювати можливості .desf без модифікації ядра інтерпретатора.
 * Вбудований дебагер: Макрос DESF_BREAKPOINT(), що генерує інструкцію int $3 (на x86) для спрацьовування системного дебагера (активний лише в режимі #ifdef DESF_DEBUG_MODE).
 * Базове шифрування команд: Приклад функції desf_encrypt_command з використанням простого XOR для демонстрації можливості захисту команд.
🛠️ Утиліта c2desf: Автоматизація перетворення
Щоб зробити процес створення .desf файлів зручним, розробляється утиліта c2desf. Її завдання – автоматично аналізувати вхідний C-код і генерувати відповідний .desf файл (тобто C-код з switch-case структурою у проміжному представленні, або безпосередньо бінарний/текстово-бінарний .desf).

 * Парсер (Frontend - Python):
   * Використовує бібліотеку libclang (Python bindings) для побудови Абстрактного Синтаксичного Дерева (AST) вхідного C-коду. Це дозволяє точно ідентифікувати всі змінні, функції, оператори та структури керування.
   * Аналізує AST для побудови Графу Потоку Керування (CFG) за допомогою NetworkX для подальших оптимізацій (напр., виявлення "мертвого коду").
   * Забезпечує кешування розпарсеного AST та надійну обробку синтаксичних помилок.
 * Обфускатор (Frontend - Python):
   * На основі AST генерує односимвольні або короткі імена, створює відповідні #define макроси (для C-генерації) або таблиці символів.
   * Застосовує багатоетапний підхід: статичне перейменування, шифрування рядкових літералів (напр., XOR з динамічним ключем), впровадження "мутного коду", Control Flow Flattening.
   * Містить опціональне шифрування рядкових літералів та інших даних.
   * Критично важливо: автоматизовані тести для перевірки функціональної еквівалентності обфускованого коду оригіналу.
 * Генератор .desf (Frontend - Python):
   * Формує кінцевий .desf файл:
     * Створює масив команд desf_commands[] (або бінарну послідовність).
     * Генерує структуру, що відповідає логіці вихідної програми (включаючи обробку умов та циклів за допомогою команд переходів та міток).
     * Додає необхідні заголовки, таблиці символів, рядків та метадані.
   * Підтримує гнучкий текстово-бінарний формат (напр., JSON-подібний для налагодження, компактний бінарний для релізів).
План досліджень та розробки c2desf (ключові аспекти)
 * Глибокий аналіз AST з libclang: Досконале вивчення можливостей libclang для точного розбору всіх конструкцій мови C, включаючи складні типи, вказівники, структури та керуючі конструкції. Для максимальної продуктивності можливий перехід на C++ API бібліотеки LLVM.
 * Алгоритми обфускації: Дослідження різних стратегій перейменування, шифрування, "розплющування" потоку керування, оцінка їх впливу на розмір, швидкість та стійкість до аналізу.
 * Представлення циклів та умов: Надійні способи трансляції циклів (for, while, do-while) та умов (if-else, switch) у послідовності команд .desf (напр., LOOP_START, LOOP_END, JUMP_IF_TRUE, JUMP_IF_FALSE, JUMP_TABLE).
 * Використання Графу Потоку Керування (CFG): Побудова CFG на основі AST для оптимізації перед генерацією команд – виявлення та видалення недосяжного коду, спрощення логічних виразів, об'єднання ідентичних блоків.
 * Автоматична оптимізація під архітектури: Вивчення можливостей генерації .desf коду, який краще використовуватиме специфічні інструкції цільового процесора (напр., векторні інструкції AVX, NEON) через спеціальні команди або метадані у .desf.
 * GUI для візуалізації AST/CFG (опціонально): Розробка графічного інтерфейсу (напр., з Dear PyGui або PyQt) для візуалізації AST/CFG, полегшення налагодження та розуміння процесу трансляції.
 * Вибір мови розробки та гібридний підхід:
   * Python (3.8+): Для фронтенду (парсер, аналізатор, обфускатор, генератор, GUI) – швидкість розробки, зручність роботи з libclang та NetworkX.
   * C (C11) / C++ (C++17): Для бекенду (високопродуктивна VM .desf, критичні модулі оптимізації).
   * Cython: Для зв'язування Python та C/C++ частин, створення ефективних Python-розширень.
⚙️ Віртуальна Машина (VM) для .desf
Серцем проекту є віртуальна машина (VM), відповідальна за виконання команд, визначених у форматі .desf. Вона написана на C/C++ для максимальної продуктивності та портативності.

Ключові модулі VM:
 * Ядро виконання (core.c):
   * Головний цикл інтерпретатора (часто switch-case по типах команд).
   * Лічильник команд (Program Counter - pc).
   * Керування станом VM (VMState).
 * Управління пам'яттю (memory.c):
   * Масив регістрів VM (vm->registers).
   * Стеки: стек викликів (call_stack) для адрес повернення, стек циклів (loop_stack) для стану циклів.
   * Можливо, управління "купою" (heap) для динамічного виділення пам'яті всередині .desf програм.
 * Арифметичні операції (ops/arithmetic.c):
   * Реалізація стандартних цілочисельних операцій (ADD, SUB, MUL, DIV, MOD, INC, DEC) для 32-бітних (та опціонально 8-бітних, 64-бітних) значень.
   * Підтримка операцій з плаваючою комою.
   * Обробка помилок: ділення на нуль (VM_ERROR_DIV_BY_ZERO), переповнення.
   * Оптимізації:
     * Векторизація (напр., op_add_vectorized з використанням ARM NEON або x86 AVX/SSE), з перевіркою вирівнювання даних та фолбеком.
     * Концептуальні заготовки для JIT-компіляції (напр., jit_compile_add_x86) з безпечним виділенням та виконанням пам'яті (mmap/VirtualAlloc).
     * Підтримка Embedded-режиму (напр., op_add_light для 8-бітних операцій).
   * Детальна Doxygen-документація.
 * Операції контролю потоку (ops/control_flow.c):
   * Керування мітками: Використання хеш-таблиці (напр., uthash.h) для зберігання відповідності між іменами міток та їх адресами (label_table). Функції add_label, find_label_address.
   * Реалізація команд:
     * JUMP: Безумовний перехід.
     * CALL: Збереження адреси повернення в call_stack, перехід.
     * RETURN: Відновлення адреси з call_stack.
     * IF/JUMP_IF_TRUE/JUMP_IF_FALSE: Умовні переходи на основі значень регістрів або результатів порівняння.
     * LOOP_START: Збереження адреси початку та лічильника ітерацій в loop_stack.
     * LOOP_END: Зменшення лічильника, перехід на початок або вихід.
   * Обробка помилок: невизначені мітки, переповнення/спустошення стеків.
 * Інші операції (ops/*.c): Робота з рядками, структурами, ввід/вивід (через абстрактний шар), виклик плагінів.
Обробка помилок у VM
 * Чітко визначені коди помилок (VMErrorCode).
 * Збереження контексту помилки: vm->error_code, vm->error_message, vm->error_address (адреса команди, що спричинила помилку).
 * Інформативні повідомлення про помилки.
⚡ Стратегії оптимізації та захисту (Поглиблено)
Проект c2desf приділяє значну увагу генерації коду, який є одночасно компактним, швидким та стійким до аналізу.
Оптимізація розміру:
 * Інтелектуальне видалення коду: Аналіз AST/CFG для усунення невикористовуваних змінних, функцій та цілих блоків коду.
 * Компактне представлення команд: Складні конструкції C (цикли, умови) трансформуються у лінійний набір спеціалізованих команд .desf.
 * Ефективне кодування (бінарний формат):
   * Коди операцій: 1-2 байти.
   * Індекси регістрів: 1 байт.
   * Малі константи: можуть бути вбудовані безпосередньо в команду.
   * Таблиці рядків та символів для уникнення дублювання.
   * Потенційна економія розміру: 30-50% порівняно з оригінальним C-кодом, і значно більше порівняно з неоптимізованими бінарними файлами.
Захист від реверс-інжинірингу (багаторівневий підхід):
 * Агресивна обфускація:
   * Перейменування: Заміна осмислених імен на короткі, неінформативні ідентифікатори (@a, F1).
   * Шифрування даних: XOR з динамічним ключем або більш стійкі алгоритми для рядків та числових констант.
   * "Розплющування" потоку керування (Control Flow Flattening): Перетворення структурованого коду на великий switch або серію умовних переходів, що ускладнює розуміння логіки.
   * Впровадження "мутного коду": Додавання надлишкових, нефункціональних команд, що не впливають на логіку, але заплутують аналізатор.
 * Динамічна логіка та інтерпретація:
   * Сам факт виконання коду через спеціалізований інтерпретатор .desf ускладнює аналіз.
   * Можливість динамічного завантаження або розшифровки блоків коду/даних "на льоту" ключем, що генерується під час запуску або залежить від середовища.
 * Анти-налагоджувальні прийоми:
   * Перевірки на наявність активного дебагера (системні виклики, таймінги).
   * Зміна поведінки програми або аварійне завершення при спробі налагодження.
   * Використання самомодифікованого коду (складно, але можливо для JIT-частин).
 * Специфікація формату: Незнання точної структури та семантики команд .desf є природним бар'єром.
Оптимізації продуктивності під конкретні архітектури:
 * Використання SIMD (Single Instruction, Multiple Data):
   * Автоматичне визначення наявності NEON (ARM) або AVX/SSE (x86) розширень.
   * Використання векторних інструкцій для паралельної обробки даних у циклах та арифметичних операціях.
   * Необхідна перевірка вирівнювання даних та наявність фолбек-реалізації.
 * JIT-компіляція (Just-In-Time):
   * Для найкритичніших до продуктивності ділянок коду ("гарячих точок").
   * Динамічна компіляція команд .desf у нативний машинний код цільової архітектури під час виконання.
   * Потребує платформо-специфічних API для виділення виконуваної пам'яті ( mmap на POSIX, VirtualAlloc на Windows) та обережного поводження з кешем інструкцій.
 * Кешування на рівні VM: Імітація L1/L2 кешу процесора для часто використовуваних даних або блоків команд.
 * Спеціалізовані команди: Деякі складні операції можуть бути реалізовані як одна оптимізована команда .desf.
Ці комплексні підходи дозволяють c2desf генерувати код, що відповідає вимогам сучасних додатків: компактність, швидкість та захищеність. Можливість налаштування оптимізацій через прапорці командного рядка (--obfuscate, --optimize=neon, --target=embedded) надає розробнику гнучкість.
🌍 Універсальність та Портативність
Однією з головних цілей проекту є забезпечення максимальної універсальності та портативності .desf коду.
Широкий спектр пристроїв:
 * Сервери та десктопи: (Linux, Windows, macOS) - повний набір оптимізацій, включаючи AVX2 та JIT.
 * Мобільні платформи (Android/iOS):
   * Компіляція інтерпретатора: Можлива за допомогою Android NDK та Xcode. Портативний C-код VM компілюється під мобільні ARM-архітектури.
   * Інтеграція: VM може бути вбудована як нативна бібліотека у мобільний додаток (через JNI на Android, Bridging Header на iOS).
   * Обмеження: Врахування пісочниці (sandboxing), доступу до ресурсів через платформо-специфічні API, обмеження на JIT (особливо на iOS). GUI потребує інтеграції з нативними компонентами або кросплатформеними фреймворками.
 * Вбудовані системи та IoT (той самий "холодильник"):
   * Спеціальний режим генерації (--target=fridge або --target=embedded).
   * Оптимізація під обмежені ресурси (напр., 8 КБ RAM).
   * 8-бітний режим операцій у VM.
   * Мінімальні залежності, відсутність потреби у стандартній бібліотеці C (завдяки desf_stdio.c та іншим абстракціям).
 * Мікроконтролери (ARM, RISC-V): Крос-компіляція VM (напр., gcc-arm-none-eabi).
Ключі до портативності:
 * Абстракція команд .desf: Високорівневі, не прив'язані до інструкцій конкретного процесора.
 * Портативний інтерпретатор (VM): Написаний на стандартному C (C11) / C++ (C++17), уникаючи специфічних розширень.
 * Мінімальні залежності: Прагнення до нульової залежності від стандартної бібліотеки C у "standalone" режимі.
 * Крос-компіляція: Використання інструментальних наборів для збірки VM під цільову платформу.
 * Тестування: Через емулятори (QEMU) та на реальному обладнанні.
🔐 Забезпечення безпеки: Протидія "Повстанню Холодильників"
Жартівливе обговорення "повстання холодильників" підняло важливе питання безпеки виконання довільного коду, особливо на підключених до мережі IoT-пристроях. Проект c2desf передбачає комплексні заходи безпеки:
 * Криптографія:
   * Шифрування команд .desf (XOR, AES) для захисту від аналізу.
   * Обов'язкове використання TLS/SSL для захисту каналів зв'язку.
 * Аутентифікація та Авторизація:
   * Строга автентифікація пристроїв та користувачів (сертифікати, токени JWT).
   * Чітке розмежування доступу до функцій за ролями та правами.
 * Ізоляція та Моніторинг:
   * Виконання підозрілих команд/процесів в ізольованому середовищі ("пісочниці").
   * Моніторинг активності та негайна зупинка ("замороження") при виявленні аномалій.
 * Обмеження функціоналу:
   * Мінімально необхідний набір прав та можливостей для пристроїв.
   * Заборона виконання коду з неперевірених джерел.
 * Безпечні оновлення:
   * Захищений механізм оновлення прошивки та ПЗ з перевіркою цифрового підпису. * Фізичний захист: Захист від фізичного доступу до пристрою та його компонентів (напр., JTAG портів).
 * "Стоп-кран" (emergency_stop): Апаратний або програмний механізм екстреної зупинки всіх "розумних" функцій, що переводить пристрій у базовий, безпечний режим.
 * "Моральний кодекс": Вбудовані на рівні VM правила, що обмежують потенційно небезпечні дії (адаптовані закони робототехніки Азімова для IoT).
📁 Структура проекту
Для забезпечення порядку, модульності та можливості подальшого розвитку була розроблена детальна структура проекту:
Управління зовнішніми залежностями
 * Ізоляція: Сторонні бібліотеки розміщуються у директорії dependencies/.
 * Документування:
   * dependencies/dependencies_rule_docs.md: Пояснює політику додавання, вимоги до ліцензій (перевага MIT/BSD/Apache), оновлення та видалення.
   * dependencies/dependency_list.md: Динамічний список усіх використаних бібліотек (назва, автор, GitHub, ліцензія, версія, призначення). Наприклад, uthash.h (BSD-2-Clause).
🚀 Встановлення та Використання (Концептуально)
Детальні інструкції будуть надані з першими релізами.
Загальний підхід до встановлення c2desf:
```
 * Клонування репозиторію:
   git clone https://github.com/Ferki-git-creator/c2desf
cd c2desf
```
 * Встановлення Python-залежностей (фронтенд):
 ```bash
   # Рекомендовано використовувати віртуальне середовище
python -m venv venv
source venv/bin/activate  # для Linux/macOS
# venv\Scripts\activate    # для Windows

# Якщо використовується Poetry
poetry install
# Або pip (якщо є requirements.txt або pyproject.toml без Poetry)
# pip install -r requirements.txt
# pip install .
```
 * Збірка C/C++ компонентів (бекенд - VM та Cython bindings):
```bash
   # Якщо використовується CMake
mkdir build && cd build
cmake ..
make
cd ..

# Або через setup_bindings.py для Cython розширень

python setup_bindings.py build_ext --inplace

Використання утиліти c2desf:
# Базовий синтаксис (концептуально)
c2desf input.c -o output.desf [OPTIONS]

# Приклади опцій:
# --obfuscate-level=high  # Рівень обфускації
# --optimize=neon         # Оптимізація під NEON
# --target=embedded       # Генерація для вбудованих систем
# --output-format=binary  # Формат вихідного файлу (binary/text)
# --debug                 # Включити налагоджувальну інформацію
```
Запуск .desf файлів за допомогою VM:
```bash
# Концептуальний запуск VM
desf_vm output.desf
```

📜 Ліцензія
Цей проект ліцензовано на умовах ліцензії MIT. Детальніше дивіться у файлі LICENSE.
Сторонні бібліотеки, що використовуються у проекті, мають власні ліцензії, які детально описані у файлі dependencies/dependency_list.md.
<div align="center">
<p><strong>Дякуємо за ваш інтерес до проекту c2desf! Разом ми можемо змінити спосіб роботи з C-кодом.</strong></p>
<p><em>"Майбутнє належить тим, хто вірить у красу своїх мрій... та пише для них ефективний код."</em> 😉</p>
</div>
